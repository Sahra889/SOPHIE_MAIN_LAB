name: 💾 Weekly Backup

on:
  schedule:
    - cron: '0 2 * * 1'  # Jeden Montag um 02:00 Uhr UTC
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v3

      - name: 📦 Upload Backup Artifact
        uses: actions/upload-artifact@v2

        with:
          name: SOPHIE_backup
          path: |
            backup/
            archive/
            snapshots/
            memory.json
            state.json
